{
  "name": "@pi-browser-bridge/chrome-extension",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@pi-browser-bridge/logger": "workspace:*",
    "@pi-browser-bridge/protocol": "workspace:*"
  },
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "test:mutation": "stryker run",
    "test:mutation:dry": "stryker run --dryRunOnly"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.4.0",
    "@stryker-mutator/core": "^9.6.1",
    "@stryker-mutator/vitest-runner": "^9.6.1",
    "vite": "^8.0.10"
  }
}
